import { Typography } from "@mui/material";
import { motion } from "framer-motion";
import { styled } from "@mui/system";

const MotionTypography = motion(styled(Typography)({}));

export default MotionTypography;
